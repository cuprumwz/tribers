syntax = "proto3"

package greeter;

import "menage.proto"

// 对外的api接口，类似restful接口
service Greeter {
    // 登签
    rpc Sign(SignPing) returns (SignPong) {}

	// 维持
    rpc Keep() returns () {}

	// 家庭信息
	rpc Lustrum(LustrumPing) returns (LustrumPong) {}
}
